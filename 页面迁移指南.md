# 📋 页面迁移实施指南

## 🎯 迁移策略

### 方案选择: 渐进式迁移(推荐)

**原因:**
1. index.html 有4800+行,包含大量JavaScript逻辑
2. 一次性重写风险太大,可能破坏现有功能
3. 渐进式迁移可以保持系统稳定运行

### 迁移步骤

#### 阶段1: 引入新CSS,保留旧样式(向后兼容)

```html
<head>
    <!-- 新增:引入统一设计系统 -->
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/animations.css">
    <link rel="stylesheet" href="/worker-selector.css">

    <!-- 保留:旧的内联样式(暂时) -->
    <style>
        /* 旧样式继续工作 */
    </style>
</head>
```

#### 阶段2: 逐步替换HTML结构

**优先级:**
1. **高优先级**: 导航栏、按钮、卡片(用户最常见)
2. **中优先级**: 表格、表单、模态框
3. **低优先级**: 特殊组件、动画效果

#### 阶段3: 删除旧样式

确认新样式工作正常后,删除 `<style>` 标签。

---

## 📝 具体迁移步骤

### index.html 迁移

#### Step 1: 更新 head 部分

**旧代码 (删除):**
```html
<style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: ...; background: ...; }
    /* 删除所有内联样式 */
</style>
```

**新代码 (添加):**
```html
<!-- 引入统一设计系统 -->
<link rel="stylesheet" href="/css/main.css">
<link rel="stylesheet" href="/css/navigation.css">
<link rel="stylesheet" href="/css/animations.css">
<link rel="stylesheet" href="/worker-selector.css">
```

#### Step 2: 添加统一导航栏

**在 `<body>` 开始处添加:**
```html
<div class="container">
    <!-- 全局导航栏 -->
    <nav class="navbar" id="main-navbar">
        <a href="/" class="navbar-brand">
            <span class="navbar-logo">🚀</span>
            <div>
                <h1 class="navbar-title">Benchmark Web Runner</h1>
                <p class="navbar-subtitle">用例驱动的性能测试平台</p>
            </div>
        </a>

        <button class="navbar-toggle" id="navbar-toggle" onclick="toggleMobileMenu()">
            <span class="navbar-toggle-bar"></span>
            <span class="navbar-toggle-bar"></span>
            <span class="navbar-toggle-bar"></span>
        </button>

        <div class="navbar-menu" id="navbar-menu">
            <a href="/" class="navbar-item active">
                <span>📋</span> 用例管理
            </a>
            <a href="/records.html" class="navbar-item">
                <span>📊</span> 测试记录
            </a>
            <a href="/workers.html" class="navbar-item">
                <span>🖥️</span> 节点管理
            </a>
            <a href="/api.html" class="navbar-item">
                <span>🔑</span> API管理
            </a>
        </div>

        <div class="navbar-actions">
            <button class="btn btn-secondary btn-sm" onclick="showGlobalConfigModal()">
                <span>⚙️</span> 全局设置
            </button>
        </div>
    </nav>

    <!-- 原有的页面头部保留,但修改样式类 -->
    <div class="header animate-fade-in-up">
        <!-- 头部内容 -->
    </div>
</div>
```

#### Step 3: 更新类名映射

| 旧类名 | 新类名 | 说明 |
|--------|--------|------|
| `.btn-primary` | `.btn .btn-primary` | 按钮 |
| `.btn-success` | `.btn .btn-success` | 成功按钮 |
| `.btn-danger` | `.btn .btn-danger` | 危险按钮 |
| `.btn-secondary` | `.btn .btn-secondary` | 次要按钮 |
| `.btn-small` | `.btn .btn-sm` | 小按钮 |
| `.status-badge` | `.badge` | 徽章 |
| `.runner-tag` | `.badge` | Runner标签 |
| `.test-cases-table` | `.table` | 表格 |

**批量替换示例:**
```bash
# 在index.html中替换(使用编辑器查找替换)
查找: class="btn-primary"
替换: class="btn btn-primary"

查找: class="status-badge
替换: class="badge
```

#### Step 4: 添加移动端菜单脚本

**在JavaScript部分添加:**
```javascript
// 移动端菜单切换
function toggleMobileMenu() {
    const toggle = document.getElementById('navbar-toggle');
    const menu = document.getElementById('navbar-menu');
    if (toggle && menu) {
        toggle.classList.toggle('active');
        menu.classList.toggle('show');
    }
}

// 导航栏滚动效果
window.addEventListener('scroll', function() {
    const navbar = document.getElementById('main-navbar');
    if (navbar) {
        if (window.scrollY > 20) {
            navbar.classList.add('navbar-scrolled');
        } else {
            navbar.classList.remove('navbar-scrolled');
        }
    }
});
```

---

### records.html 迁移

#### Step 1: 更新 head

```html
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>测试记录 - Benchmark Web Runner</title>

    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>

    <!-- 统一设计系统 -->
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/animations.css">
</head>
```

#### Step 2: 添加统一导航

同 index.html,但 active 状态不同:

```html
<a href="/" class="navbar-item">📋 用例管理</a>
<a href="/records.html" class="navbar-item active">📊 测试记录</a>
<a href="/workers.html" class="navbar-item">🖥️ 节点管理</a>
```

#### Step 3: 更新类名

| 旧类名 | 新类名 |
|--------|--------|
| `.records-table` | `.table` |
| `.btn-download` | `.btn .btn-success .btn-sm` |
| `.perfcat-link` | `.btn .btn-primary .btn-sm` |

---

### workers.html 迁移

#### Step 1: 更新背景

**删除纯色背景,改用渐变:**

```html
<!-- 旧: background: #f5f7fa; -->
<!-- 新: 使用 main.css 的 body 渐变背景 -->
```

#### Step 2: 更新类名

| 旧类名 | 新类名 |
|--------|--------|
| `.worker-card` | `.card` |
| `.action-btn.edit` | `.btn .btn-primary .btn-sm` |
| `.action-btn.delete` | `.btn .btn-danger .btn-sm` |
| `.stat-value.online` | `.stat-value .text-success` |

---

## 🛠️ 自动化迁移工具

### 批量替换脚本

创建 `/Users/bilibili/benchmark/migrate-classes.sh`:

```bash
#!/bin/bash

# 备份文件
echo "Creating backups..."
cp public/index.html backup/index-pre-migrate.html
cp public/records.html backup/records-pre-migrate.html
cp public/workers.html backup/workers-pre-migrate.html

# 替换按钮类名
echo "Migrating button classes..."
sed -i.bak 's/class="btn-primary"/class="btn btn-primary"/g' public/index.html
sed -i.bak 's/class="btn-success"/class="btn btn-success"/g' public/index.html
sed -i.bak 's/class="btn-danger"/class="btn btn-danger"/g' public/index.html
sed -i.bak 's/class="btn-secondary"/class="btn btn-secondary"/g' public/index.html
sed -i.bak 's/class="btn-small"/class="btn btn-sm"/g' public/index.html

# 替换徽章类名
echo "Migrating badge classes..."
sed -i.bak 's/class="status-badge/class="badge/g' public/*.html
sed -i.bak 's/class="runner-tag/class="badge/g' public/*.html

# 清理临时文件
rm public/*.bak

echo "Migration complete!"
```

---

## ✅ 迁移检查清单

### index.html

- [ ] 引入新CSS文件
- [ ] 添加统一导航栏
- [ ] 更新按钮类名
- [ ] 更新徽章类名
- [ ] 更新表格类名
- [ ] 添加移动端菜单脚本
- [ ] 测试所有功能正常
- [ ] 删除内联 `<style>` 标签

### records.html

- [ ] 引入新CSS文件
- [ ] 添加统一导航栏(active: 测试记录)
- [ ] 更新类名
- [ ] 测试图表显示正常
- [ ] 测试筛选功能
- [ ] 删除内联样式

### workers.html

- [ ] 引入新CSS文件
- [ ] 添加统一导航栏(active: 节点管理)
- [ ] 更新背景为渐变
- [ ] 更新Worker卡片类名
- [ ] 更新按钮类名
- [ ] 测试实时更新功能
- [ ] 删除内联样式

---

## 🧪 测试计划

### 功能测试

#### index.html
1. ✅ 添加/编辑/删除用例
2. ✅ 运行测试
3. ✅ 停止测试
4. ✅ 导入/导出用例
5. ✅ WebSocket实时更新
6. ✅ Worker选择器
7. ✅ Cookie自动获取
8. ✅ 模态框打开/关闭

#### records.html
1. ✅ 加载测试记录
2. ✅ 筛选功能
3. ✅ 分页功能
4. ✅ 图表显示
5. ✅ 下载日志
6. ✅ 删除记录

#### workers.html
1. ✅ 显示Worker列表
2. ✅ 实时状态更新
3. ✅ 编辑Worker
4. ✅ 删除Worker

### 兼容性测试

- [ ] Chrome (最新版)
- [ ] Firefox (最新版)
- [ ] Safari (最新版)
- [ ] Edge (最新版)
- [ ] 移动端 Chrome
- [ ] 移动端 Safari

### 响应式测试

- [ ] 桌面 (1920×1080)
- [ ] 笔记本 (1366×768)
- [ ] 平板 (768×1024)
- [ ] 手机 (375×667)

---

## 🎯 迁移时间表

| 阶段 | 任务 | 预计时间 | 状态 |
|------|------|----------|------|
| **准备** | 备份文件 | 5分钟 | ✅ 完成 |
| **阶段1** | index.html 添加CSS | 10分钟 | ⏳ 进行中 |
| **阶段2** | index.html 更新类名 | 30分钟 | ⏸️ 待开始 |
| **阶段3** | index.html 测试 | 20分钟 | ⏸️ 待开始 |
| **阶段4** | records.html 迁移 | 30分钟 | ⏸️ 待开始 |
| **阶段5** | workers.html 迁移 | 20分钟 | ⏸️ 待开始 |
| **阶段6** | 删除旧样式 | 10分钟 | ⏸️ 待开始 |
| **阶段7** | 全面测试 | 30分钟 | ⏸️ 待开始 |
| **总计** | | **2.5小时** | |

---

## 📞 问题处理

### 常见问题

**Q1: 新样式和旧样式冲突怎么办?**
A1: CSS优先级问题。新CSS在旧样式之后引入,应该会覆盖。如果不行,在新CSS中使用 `!important`。

**Q2: JavaScript功能失效?**
A2: 检查类名是否正确更新。某些JS可能依赖特定类名。

**Q3: 布局错乱?**
A3: 检查是否有旧的内联样式覆盖新样式。逐步删除旧样式,观察变化。

**Q4: 移动端显示异常?**
A4: 检查响应式类名是否正确。使用浏览器开发者工具调试。

---

## 🚀 快速迁移脚本

为了加快迁移速度,我将创建一个自动化脚本来处理大部分工作。

**使用方法:**
```bash
cd /Users/bilibili/benchmark
bash migrate-pages.sh
```

**脚本功能:**
1. 自动备份文件
2. 在 `<head>` 中添加新CSS引用
3. 批量替换类名
4. 添加统一导航栏
5. 添加必要的JavaScript函数

---

**状态**: 📝 迁移指南已创建
**下一步**: 执行实际迁移
