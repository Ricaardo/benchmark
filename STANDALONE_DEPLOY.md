# 单机独立部署指南

## 概述

本指南介绍如何在**每台机器上独立部署** Benchmark 系统，适用于**机器之间无法通过 SSH 连接**的场景。

## 🎯 适用场景

- ✅ 机器分散在不同位置，无法统一远程管理
- ✅ 不希望配置 SSH 免密登录
- ✅ 每台机器单独部署和管理
- ✅ 支持 Linux / macOS / Windows

## 📋 部署流程

### 整体思路

1. **第一台机器**：部署为 Master 节点（主控服务器）
2. **其他机器**：部署为 Worker 节点，连接到 Master

### 详细步骤

```
┌─────────────┐
│  机器 1     │  → 运行部署脚本 → 选择 Master → 获得 IP:Port
│  (Master)   │
└──────┬──────┘
       │
       │ 连接
       │
   ┌───┴────────┬──────────┬──────────┐
   │            │          │          │
┌──▼──┐    ┌──▼──┐    ┌──▼──┐    ┌──▼──┐
│机器2│    │机器3│    │机器4│    │机器5│
│Worker│   │Worker│   │Worker│   │Worker│
└─────┘    └─────┘    └─────┘    └─────┘
  ↑运行部署脚本 → 选择 Worker → 输入 Master IP
```

## 🚀 快速开始

### 前置要求

**所有机器都需要：**

1. **Node.js >= 18.0.0**

   **macOS:**
   ```bash
   brew install node
   ```

   **Ubuntu/Debian:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

   **Windows:**
   下载安装：https://nodejs.org

2. **项目代码**

   在每台机器上获取项目代码（任选一种方式）：

   ```bash
   # 方式1: Git 克隆
   git clone <repository-url>
   cd benchmark

   # 方式2: 下载压缩包并解压
   # 方式3: U盘拷贝
   ```

---

### 第一步：部署 Master 节点

在**第一台机器**上运行：

#### Linux / macOS

```bash
cd benchmark
./scripts/standalone-deploy.sh
```

#### Windows

```batch
cd benchmark
scripts\standalone-deploy.bat
```

**交互式配置：**

```
选择部署角色:
  1) Master 节点 (主控服务器 + Web UI)
  2) Worker 节点 (测试执行节点)
  3) Master + Worker (同机部署)
  4) 取消

请输入选项 [1-4]: 1   ← 选择 Master

Web 服务端口 [默认: 3000]: 3000   ← 直接回车使用默认

检测到本机 IP: 192.168.1.100   ← 记下这个 IP！

是否全局安装 PM2？[y/n]: y   ← 推荐安装 PM2
```

**部署完成后：**

```
╔════════════════════════════════════════════════════════╗
║                    部署成功！                          ║
╚════════════════════════════════════════════════════════╝

📡 访问地址:
   本机: http://localhost:3000
   局域网: http://192.168.1.100:3000   ← 其他 Worker 连接这个地址

📋 下一步:
   在其他机器上运行此脚本，选择 Worker 模式
   Worker 连接到: http://192.168.1.100:3000
```

**重要：记录 Master 的访问地址！** 其他 Worker 需要这个地址。

---

### 第二步：部署 Worker 节点

在**每台 Worker 机器**上分别运行：

#### Linux / macOS

```bash
cd benchmark
./scripts/standalone-deploy.sh
```

#### Windows

```batch
cd benchmark
scripts\standalone-deploy.bat
```

**交互式配置：**

```
选择部署角色:
  1) Master 节点 (主控服务器 + Web UI)
  2) Worker 节点 (测试执行节点)
  3) Master + Worker (同机部署)
  4) 取消

请输入选项 [1-4]: 2   ← 选择 Worker

➤ 正在扫描局域网，寻找 Master 服务器...   ← 自动扫描
ℹ️  检测到本机网段: 192.168.1.x
ℹ️  正在扫描常见 IP...

  ✓ 发现 Master: http://192.168.1.100:3000   ← 自动发现

✅ 找到 1 个 Master 服务器

请选择要连接的 Master:
  1) http://192.168.1.100:3000   ← 直接选择
  2) 手动输入其他地址

请选择 [1-2]: 1   ← 选择自动发现的 Master
✅ 已选择: http://192.168.1.100:3000

Worker 名称 [默认: Worker-hostname]: 高配机-1   ← 自定义名称

请选择性能等级:
  1) high   - 高配 (16核+, 32GB+)
  2) medium - 中配 (4-8核, 8-16GB) [推荐]
  3) low    - 低配 (2-4核, 4-8GB)
  4) custom - 自定义

请选择 [1-4]: 2   ← 选择性能等级

描述信息 [默认: x86_64 - Linux]: 8核16GB Ubuntu 22.04   ← 可选

标签 (逗号分隔) [可选]: testing,linux   ← 可选

是否全局安装 PM2？[y/n]: y   ← 推荐安装
```

**部署完成后：**

```
╔════════════════════════════════════════════════════════╗
║                    部署成功！                          ║
╚════════════════════════════════════════════════════════╝

🔧 Worker 信息:
   名称: 高配机-1
   性能等级: medium
   连接到: http://192.168.1.100:3000

💡 提示:
   访问 Master Web UI 查看此 Worker 是否已连接
```

---

### 第三步：验证部署

访问 Master Web UI：

```
http://192.168.1.100:3000
```

在页面上应该能看到所有 Worker 节点已连接。

---

## 📝 配置说明

### 自动生成的配置文件

部署脚本会在项目根目录生成 `.deploy-local.json` 配置文件：

**Master 配置示例：**
```json
{
  "role": "master",
  "service_port": 3000,
  "local_ip": "192.168.1.100",
  "configured_at": "2025-01-15 10:30:00"
}
```

**Worker 配置示例：**
```json
{
  "role": "worker",
  "master_url": "http://192.168.1.100:3000",
  "worker_name": "高配机-1",
  "performance_tier": "medium",
  "description": "8核16GB Ubuntu 22.04",
  "tags": "testing,linux",
  "configured_at": "2025-01-15 10:35:00"
}
```

### 重新配置

如果需要修改配置，重新运行部署脚本：

```bash
./scripts/standalone-deploy.sh
```

脚本会检测到已有配置并询问是否重新配置。

---

## 🎮 服务管理

### 使用 PM2 管理（推荐）

如果安装了 PM2，可以使用以下命令：

**查看状态：**
```bash
pm2 status
```

**查看日志：**
```bash
# Master
pm2 logs benchmark-master

# Worker
pm2 logs benchmark-worker-medium
```

**重启服务：**
```bash
# Master
pm2 restart benchmark-master

# Worker
pm2 restart benchmark-worker-medium
```

**停止服务：**
```bash
# Master
pm2 stop benchmark-master

# Worker
pm2 stop benchmark-worker-medium
```

**开机自启：**
```bash
pm2 startup
pm2 save
```

### 不使用 PM2

如果没有安装 PM2：

**Linux/macOS:**
- 服务运行在后台
- 查看日志：`tail -f logs/master.log` 或 `logs/worker.log`
- 停止服务：`kill $(cat .master.pid)` 或 `kill $(cat .worker.pid)`

**Windows:**
- 服务运行在最小化窗口
- 停止服务：关闭对应的命令行窗口或使用任务管理器

---

## 🔧 常见问题

### 1. Worker 无法连接到 Master

**问题：** Worker 部署时提示"无法连接到 Master"

**排查步骤：**

1. **确认 Master 正在运行**
   ```bash
   # 在 Master 机器上
   curl http://localhost:3000
   ```

2. **确认防火墙允许端口 3000**
   ```bash
   # Ubuntu/Debian
   sudo ufw allow 3000/tcp
   sudo ufw status

   # CentOS/RHEL
   sudo firewall-cmd --add-port=3000/tcp --permanent
   sudo firewall-cmd --reload

   # Windows
   # 在"Windows Defender 防火墙"中添加入站规则允许端口 3000
   ```

3. **确认 IP 地址正确**
   ```bash
   # 在 Master 机器上查看 IP
   # Linux/macOS
   ifconfig
   ip addr

   # Windows
   ipconfig
   ```

4. **从 Worker 机器测试连接**
   ```bash
   curl http://192.168.1.100:3000
   ping 192.168.1.100
   ```

### 2. Node.js 版本过低

**问题：** 提示"Node.js 版本过低"

**解决：** 升级 Node.js 到 18.0.0 或更高版本

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# macOS
brew upgrade node

# Windows
# 从 https://nodejs.org 下载最新版本重新安装
```

### 3. 端口已被占用

**问题：** 端口 3000 被占用

**解决方案：**

```bash
# 查找占用进程
# Linux/macOS
lsof -i :3000
netstat -tulnp | grep 3000

# Windows
netstat -ano | findstr :3000

# 停止占用进程或在部署时选择其他端口
```

### 4. PM2 启动失败

**问题：** PM2 无法启动或找不到

**解决方案：**

1. **确认 PM2 已全局安装**
   ```bash
   npm install -g pm2
   ```

2. **检查权限**
   ```bash
   # Linux/macOS 可能需要 sudo
   sudo npm install -g pm2
   ```

3. **不使用 PM2**

   在部署时选择 `n` 跳过 PM2 安装

### 5. 依赖安装失败

**问题：** npm install 失败

**解决方案：**

1. **清理缓存**
   ```bash
   npm cache clean --force
   rm -rf node_modules package-lock.json
   npm install
   ```

2. **使用国内镜像（中国大陆）**
   ```bash
   npm config set registry https://registry.npmmirror.com
   npm install
   ```

3. **检查网络连接**

---

## 📊 部署检查清单

### Master 节点

- [ ] Node.js >= 18.0.0 已安装
- [ ] 项目代码已下载
- [ ] 运行 `standalone-deploy.sh` 或 `standalone-deploy.bat`
- [ ] 选择 Master 角色
- [ ] 记录 Master IP 和端口
- [ ] 访问 Web UI 确认服务正常
- [ ] 防火墙开放端口 3000

### Worker 节点（每台）

- [ ] Node.js >= 18.0.0 已安装
- [ ] 项目代码已下载
- [ ] 运行 `standalone-deploy.sh` 或 `standalone-deploy.bat`
- [ ] 选择 Worker 角色
- [ ] 输入 Master IP 和端口
- [ ] 连接测试成功
- [ ] 配置 Worker 名称和性能等级
- [ ] 在 Master Web UI 确认 Worker 已连接

---

## 🔄 更新部署

当代码更新后，在**每台机器上**分别操作：

### 使用 Git（推荐）

```bash
cd benchmark
git pull
./scripts/standalone-deploy.sh
# 选择相同的角色，脚本会自动更新
```

### 手动替换代码

1. 备份配置文件：
   ```bash
   cp .deploy-local.json .deploy-local.json.bak
   ```

2. 替换新代码

3. 恢复配置：
   ```bash
   cp .deploy-local.json.bak .deploy-local.json
   ```

4. 重新部署：
   ```bash
   ./scripts/standalone-deploy.sh
   ```

---

## 💡 最佳实践

### 1. 命名规范

建议为 Worker 使用有意义的名称：

```
- 高配机-办公室
- 中配机-测试环境
- 低配机-兼容性测试
- MacBook-Pro-M2
- Windows-服务器-1
```

### 2. 性能等级选择

| 性能等级 | CPU | 内存 | 适用场景 |
|---------|-----|------|---------|
| high | 16核+ | 32GB+ | 性能压测、复杂场景 |
| medium | 4-8核 | 8-16GB | 日常测试、回归测试（推荐）|
| low | 2-4核 | 4-8GB | 兼容性测试、轻量场景 |
| custom | 自定义 | 自定义 | 特殊需求 |

### 3. 标签管理

使用标签便于筛选和管理：

```
- os: linux, macos, windows
- env: production, testing, development
- location: office, datacenter, home
- purpose: performance, compatibility, stress
```

### 4. 开机自启

使用 PM2 并配置开机自启：

```bash
pm2 startup
pm2 save
```

### 5. 日志管理

定期清理日志文件：

```bash
# PM2 日志
pm2 flush

# 项目日志
rm -f logs/*.log
```

---

## 📞 获取帮助

遇到问题请查看：

1. 本文档常见问题章节
2. [README.md](README.md) - 主文档
3. [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - 故障排查指南
4. GitHub Issues - 提交问题反馈

---

## 🎉 部署成功后

访问 Master Web UI 开始使用：

```
http://192.168.1.100:3000
```

功能：
- ✅ 创建测试用例
- ✅ 选择 Worker 执行测试
- ✅ 实时查看测试进度
- ✅ 查看测试报告
- ✅ 批量分发任务
- ✅ 监控 Worker 状态

---

**祝你部署顺利！** 🚀
